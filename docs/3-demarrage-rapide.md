# Démarrage Rapide

Une fois le projet installé, vous pouvez lancer le serveur de développement.

### Lancer le serveur

**Si vous utilisez l'option A (TypeScript avec Express) :**
```bash
npm run dev
